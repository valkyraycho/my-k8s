apiVersion: v1
kind: Pod
metadata:
  name: web
spec:
  containers:
  - name: server
    image: busybox
    command: ["sh", "-c", "echo 'hello from web pod' > /tmp/index.html && httpd -f -p 8080 -h /tmp"]